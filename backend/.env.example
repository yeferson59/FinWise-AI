# FinWise-AI Backend Environment Configuration
# Copy this file to .env and fill in your actual values
# DO NOT commit .env file with real credentials

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================
APP_NAME=FinWise API
PORT=8000
ENVIRONMENT=development
VERSION=1.0.0
PREFIX_API=/api/v1

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# SQLite (Development)
DATABASE_URL=sqlite:///database.db

# PostgreSQL (Production - Recommended)
# DATABASE_URL=postgresql://user:password@localhost:5432/finwise

# =============================================================================
# SECURITY & AUTHENTICATION
# =============================================================================
# Generate a secure random secret key with:
# python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-super-secret-key-change-in-production-min-32-chars
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# =============================================================================
# AI/LLM CONFIGURATION
# =============================================================================
# OpenRouter API Key (get from https://openrouter.ai/)
# Format: sk-or-v1-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
OPENAI_API_KEY=your-openrouter-api-key-here

# AI Model Configuration
# Available models:
# - nvidia/llama-3.3-nemotron-70b-instruct (default, high quality)
# - meta-llama/llama-3.3-70b-instruct
# - openai/gpt-4-turbo
# - anthropic/claude-3-opus
MODELS=nvidia/llama-3.3-nemotron-70b-instruct

# Model Parameters
TEMPERATURE=0.2
TOP_P=0.3

# =============================================================================
# FILE STORAGE
# =============================================================================
# Storage Type: 'local' or 's3'
FILE_STORAGE_TYPE=local
LOCAL_STORAGE_PATH=uploads

# AWS S3 Configuration (Optional - for production)
# S3_BUCKET=your-bucket-name
# S3_REGION=us-east-1
# S3_ACCESS_KEY=your-access-key
# S3_SECRET_KEY=your-secret-key
# S3_ENDPOINT=https://s3.amazonaws.com

# =============================================================================
# NOTES
# =============================================================================
# 1. SECRET_KEY should be at least 32 characters and randomly generated
# 2. Never commit your .env file to version control
# 3. Use different SECRET_KEY values for each environment
# 4. For production, use PostgreSQL instead of SQLite
# 5. Store production secrets in a secure secrets manager (AWS Secrets Manager, etc.)
